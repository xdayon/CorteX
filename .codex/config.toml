# Keep routine repository work on the economical model. A human or orchestrator
# can override `-m` for architecture, difficult debugging, or final review.
model = "gpt-5.4-mini"
model_reasoning_effort = "low"
personality = "pragmatic"
